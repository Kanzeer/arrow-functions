//Questão 01: Escreva uma arrow function chamada dobro que recebe um número como parâmetro e retorna o dobro desse número.

const quadrado = (x) => {
    return x * 2;
};
console.log(quadrado(2)); //saída 4